import React from 'react';
import {Button} from "@nextui-org/button";

const RevealButton = () => {
    return (
        <div className={"flex justify-center"}>
            <Button color="primary" variant="shadow">Reveal Votes</Button>
        </div>
    );
};

export default RevealButton;
